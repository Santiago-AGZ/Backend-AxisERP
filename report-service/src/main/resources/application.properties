spring.application.name=report-service

# DATABASE
spring.datasource.url=${REPORT_DB_URL}
spring.datasource.username=${REPORT_DB_USERNAME}
spring.datasource.password=${REPORT_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# HIKARICP — Neon serverless pooler
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SERVER
server.port=8085

# ACTUATOR
management.endpoints.web.exposure.include=health,info

# JWT / SUPABASE AUTH (ES256 via JWKS)
jwt.jwks-uri=${SUPABASE_JWT_ISSUER}/.well-known/jwks.json

# INTER-SERVICE AUTH
internal.api.key=${INTERNAL_API_KEY}

# SERVICE URLS
inventory.service.url=${INVENTORY_SERVICE_URL}
catalog.service.url=${CATALOG_SERVICE_URL}
sales.service.url=${SALES_SERVICE_URL}
purchase.service.url=${PURCHASE_SERVICE_URL}
