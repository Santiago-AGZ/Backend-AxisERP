spring.application.name=report-service

# DATABASE
spring.datasource.url=${report-db-url}
spring.datasource.username=${report-db-username}
spring.datasource.password=${report-db-password}
spring.datasource.driver-class-name=org.postgresql.Driver

# HIKARICP — Neon serverless pooler
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=${jpa-ddl-auto:update}
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SERVER
server.port=${server-port:8085}

# ACTUATOR
management.endpoints.web.exposure.include=health,info

# JWT / SUPABASE AUTH (ES256 via JWKS)
jwt.jwks-uri=${supabase-jwt-issuer}/.well-known/jwks.json

# INTER-SERVICE AUTH
internal.api.key=${internal-api-key}

# SERVICE URLS
inventory.service.url=${inventory-service-url}
catalog.service.url=${catalog-service-url}
sales.service.url=${sales-service-url}
purchase.service.url=${purchase-service-url}
